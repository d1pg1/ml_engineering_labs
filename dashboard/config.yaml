mlflow:
  tracking_uri: "outputs/mlruns"
  experiment_name: "cifar10_mlflow"

data:
  data_dir: "data"
  images_dir: "data/images"
  labels_path: "data/cifar-10-batches-py"
  test_size: 0.2
  val_size: 0.2
  random_state: 42
  n_classes: 10

model:
  checkpoint_path: "outputs/lab04_main.pth"
  n_classes: 10
  batch_size: 64
  num_workers: 0
